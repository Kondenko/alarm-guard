<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="preferenceTheme">@style/CustomPreferenceTheme</item>
    </style>

    <style name="CustomPreferenceTheme" parent="PreferenceThemeOverlay">
        <item name="preferenceFragmentListStyle">@style/CustomListTheme</item>
    </style>

    <style name="CustomListTheme" >
        <item name="android:paddingLeft">0dp</item>
        <item name="android:paddingRight">0dp</item>
    </style>

</resources>
